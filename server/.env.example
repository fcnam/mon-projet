# ========================================
# AIBVS Backend - Configuration Environment
# ========================================

# Port d'écoute du serveur Express
PORT=5000

# Secret JWT pour la signature des tokens d'authentification
# ⚠️ IMPORTANT: Changez cette valeur en production par une chaîne aléatoire sécurisée
# Pour générer un secret sécurisé, utilisez:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=votre_secret_jwt_tres_securise_changez_moi_en_production_123456789

# Environnement d'exécution
# Valeurs possibles: development, production, test
NODE_ENV=development

# Durée de validité du token JWT (optionnel)
# Exemples: 1h, 24h, 7d, 30d
# JWT_EXPIRES_IN=24h

# Nombre de rounds pour le hachage bcrypt (optionnel, défaut: 10)
# Plus le nombre est élevé, plus le hachage est sécurisé mais lent
# BCRYPT_ROUNDS=10

# Nom du fichier de base de données SQLite (optionnel, défaut: aibvs.db)
# DB_NAME=aibvs.db

# Configuration CORS (optionnel)
# CORS_ORIGIN=http://localhost:5173

# Niveau de logs (optionnel)
# LOG_LEVEL=info